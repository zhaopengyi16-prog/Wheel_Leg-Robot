# ROS2 工作区

本目录用于保存 ROS2 系统代码。当前目录为空。

## 建议结构

```text
ros2_ws/
└── src/
    └── 待确认
```

## 当前状态

- ROS2 发行版：待确认。
- 工作区包列表：待确认。
- 构建工具：待确认。
- 当前 `build/`、`install/`、`log/` 目录：未发现。

## 注意事项

- `build/`、`install/`、`log/` 为构建输出，不应纳入版本管理。
- 当前发现的 ROS2 描述包位于 `mechanical/robot1_description/`，是否迁移到本工作区待确认。
